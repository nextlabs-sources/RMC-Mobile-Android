<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="@dimen/dp180"
        android:layout_above="@+id/bt_select_repo"
        android:layout_centerVertical="true"
        android:background="@android:color/transparent"
        android:contentDescription="@string/no_repo_connect"
        android:scaleType="fitCenter"
        android:src="@drawable/repo_empty_state" />

    <Button
        android:id="@+id/bt_select_repo"
        style="?android:attr/buttonBarButtonStyle"
        android:layout_width="@dimen/dp1"
        android:layout_height="@dimen/dp1"
        android:layout_centerInParent="true"
        android:layout_gravity="center_horizontal"
        android:background="@drawable/repository_activity_add_repository_button_background2"
        android:padding="@dimen/dp5"
        android:text="@string/go_select_repository"
        android:textColor="@android:color/white"
        android:visibility="invisible" />

    <TextView
        android:id="@+id/nxfile_norepo_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/bt_select_repo"
        android:layout_marginTop="10dp"
        android:gravity="center"
        android:padding="30dp"
        android:text="@string/no_repo_connect"
        android:textColor="@color/sub_text_color"
        android:textSize="@dimen/text_size_16sp" />

</RelativeLayout>